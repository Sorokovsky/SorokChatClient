import styles from "@/styles/index.module.sass";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className={styles.main}>
     Hello World
    </main>
  );
};

export default Home;